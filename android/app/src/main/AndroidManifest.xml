<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ExpenseApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_mic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_mic"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseRecorder">

        <!-- Main expense log activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Transparent activity that handles voice recording from widget -->
        <activity
            android:name=".RecordingActivity"
            android:exported="false"
            android:theme="@style/Theme.Transparent"
            android:launchMode="singleTop"
            android:excludeFromRecents="true" />

        <!--
            MSAL browser tab redirect.
            Replace YOUR_BASE64_SIGNATURE_HASH_HERE with the base64 SHA1 hash
            of your signing certificate (shown in Azure Portal when registering the app).
        -->
        <activity
            android:name="com.microsoft.identity.client.BrowserTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="msauth"
                    android:host="com.maru.expenserecorder"
                    android:path="/YOUR_BASE64_SIGNATURE_HASH_HERE" />
            </intent-filter>
        </activity>

        <!-- Home screen widget -->
        <receiver
            android:name=".ExpenseWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/expense_widget_info" />
        </receiver>

    </application>
</manifest>
